name: CICD

on:
  push:
    branches: [deploy-to-ec2]


jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
      - name: Checkout source
        uses: actions/checkout@v3
      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Build Project
        run: mvn clean install -DskipTests
      - name: Login to docker hub
        run: docker login -u ${{secrets.DOCKER_USERNAME}} -p ${{secrets.DOCKER_PASSWORD}}
      - name: Build docker image
        run: docker build -t shivam431/gift-orchids-aws .
      - name: Publish image to docker hub
        run: docker push shivam431/gift-orchids-aws:latest

  deploy:
    needs: build
    runs-on: [aws-ec2]
    steps:
      - name: Pull Image from docker hub
        run: sudo docker pull shivam431/gift-orchids-aws:latest
      - name: Delete old container
        run: sudo docker rm -f gift-orchids-aws-container
      - name: Run docker container
        run: sudo docker run -d -p 8080:8080 --name gift-orchids-aws-container shivam431/gift-orchids-aws

