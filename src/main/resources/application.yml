spring:
  datasource:
    url: jdbc:postgresql://3.110.167.0:5432/postgres
    username: postgres
    password: Shivam@12345
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

aws:
  region: ap-south-1
  s3:
    bucket: giftorchidsbucket
